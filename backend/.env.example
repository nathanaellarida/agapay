# ---- Pick ONE provider ----

# Google Gemini (default)
GOOGLE_API_KEY=your_google_api_key_here

# OpenAI (optional — uncomment the OpenAI branch in rag_chain.py to use)
# OPENAI_API_KEY=your_openai_api_key_here

# Which provider to use: "gemini" or "openai"
LLM_PROVIDER=gemini

# Paths (defaults are fine)
DATA_DIR=./data
CHROMA_DIR=./chroma_db
COLLECTION_NAME=agapay_msme
